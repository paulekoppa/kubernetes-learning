# serviceaccount-node-reader.yaml
#
# Dedicated identity for a workload that needs to read cluster-wide
# Node information - for example, a diagnostic or monitoring pod.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: node-reader-sa
  namespace: project-6
